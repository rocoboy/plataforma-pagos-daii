{
  "$schema": "https://railway.com/schemas/railway.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "bun install && bun run build"
  },
  "deploy": {
    "startCommand": "bun run dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}

